syntax = "proto3"; // 协议版本
option java_outer_classname = "StudentPOJO";//生成的外部类名同时也是文件名
option java_package = "netty.codec"; // 编译后的文件存储的包的路径
message Student{//这个类是外部类 StudentPOJO 的内部类
  int32 id = 1;// Student 类中有一个属性名 id，类型是 int32，这里的 1 是指属性序号
  string name = 2;
}
